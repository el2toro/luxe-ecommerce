export interface OrderItemDetailsModel {
  id: string;
  productId: string;
  productName: string;
  productImageUrl: string;
  productSku: string;
  unitPrice: number;
  quantity: number;
  discount: number;
}
